import { StyleSheet } from 'react-native'

export const style = StyleSheet.create({
    serviceLabel: {
        fontWeight: 'bold',
        fontSize: 20,
        color: '#000',
        textAlign: 'left',
        marginLeft: 10,
        marginBottom: 10  
    },
    flatListView:{
        height: 580
    }
})